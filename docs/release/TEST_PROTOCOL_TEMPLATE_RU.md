# Протокол технической проверки релиза

- Release SHA: [ТРЕБУЕТ ФИКСАЦИИ РЕЛИЗА]
- Архив/SHA-256: [ГЕНЕРИРУЕТСЯ ДЛЯ РЕЛИЗА]
- Дата, стенд, исполнитель: [ТРЕБУЕТ ЗАПОЛНЕНИЯ]

| Проверка | Команда/сценарий | Ожидается | Результат/лог |
|---|---|---|---|
| Backend unit/integration | `pytest` с отдельным PostgreSQL | все обязательные тесты проходят | [ТРЕБУЕТ ПРОГОНА CI] |
| Frontend types | `pnpm run check` | exit 0 | [ТРЕБУЕТ ПРОГОНА CI] |
| Frontend build | `pnpm run build` | exit 0 | [ТРЕБУЕТ ПРОГОНА CI] |
| Миграции | чистая БД, upgrade до head | exit 0 | [ТРЕБУЕТ ПРОГОНА] |
| Health/readiness | documented endpoints | успешный ответ | [ТРЕБУЕТ ПРОГОНА] |
| Backup/restore | dump → новый контур → smoke | данные и функции восстановлены | [ТРЕБУЕТ ПРОГОНА] |
| Secret/client scan | `legal_release_kit.py audit/all` | нет блокирующих находок в архиве | [ГЕНЕРИРУЕТСЯ] |
| Независимая установка | чек-лист отдельным исполнителем | без доступа разработчика | [ТРЕБУЕТ ПРОГОНА] |

Логи хранятся как приложение к Акту. Утверждения README не заменяют лог конкретного commit.
