## Linear

Linear: https://linear.app/pu-workspace-ai/issue/PU-0/replace-with-issue-slug

The PR title must start with `[PU-N]`, and the branch must be named
`codex/pu-N-short-description`. Replace `PU-0` above and `PU-N` in both PR fields with the
same real issue key. The required CI rejects placeholders and mismatched keys.

## What changed

-

## Acceptance evidence

- [ ] Acceptance criteria from the Linear issue are covered.
- [ ] Relevant local tests passed.
- [ ] No production secrets or customer documents were added.
- [ ] No external action was performed unless the Linear issue explicitly authorized it.

## Verification after merge

- [ ] All required GitHub checks passed on the exact merge candidate.
- [ ] The tested environment was verified before the Linear issue is closed.
